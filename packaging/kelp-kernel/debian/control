Source: kelp-kernel
Section: kernel
Priority: optional
Maintainer: Kelp Linux Team <team@kelp.linux>
Build-Depends: debhelper-compat (= 13), dkms, linux-headers-generic
Standards-Version: 4.6.2

Package: kelp-kernel
Architecture: all
Depends: ${misc:Depends},
 dkms,
 linux-headers-generic | linux-headers
Description: Kelp Kernel — AI-aware Linux kernel module (DKMS)
 Out-of-tree Linux kernel module that provides /dev/kelp for userspace
 IPC, /proc/kelp for runtime statistics, and netfilter hooks for
 network-aware AI analysis. Installs via DKMS for automatic rebuilds
 on kernel updates.
